# Media control

XF86AudioPlay
  playerctl play-pause

XF86AudioNext
  playerctl next

XF86AudioPrev
  playerctl previous

Print
  maim -m 10 | xclip -sel c -t image/png

alt + Print
  maim -s -m 10 | xclip -sel c -t image/png

shift + Print
  var="$HOME/Pictures/screenshots/$(date +%d-%m-%y--%R-%M-%S-%p).png"; \
  maim -m 10 "$var"; \
  xclip -sel c -t image/png "$var"

alt + space
  ~/.scripts/special-characters/saccent
  
